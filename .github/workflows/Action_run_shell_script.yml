on:
  push:
    branches:
    - main
    
jobs:
  build:
    name: Run Bash on commit, new file
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Call a One Line bash script
        run:  echo "this be running a one line bash script"
